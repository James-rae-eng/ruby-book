<h1>Editing post</h1>

<%= render "form", post: @post %>

<br>

<div>
  <%#= button_to "Destroy this post", @post, method: :delete %>
  <%= link_to "Destroy this post", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</div>

<div>
  <%= link_to "Back to posts", posts_path %>
</div>
