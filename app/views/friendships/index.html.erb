
<h1><%= "Your Friends" %></h1>

<% current_user.friends.each do |friend| %>
  <li>
    <%= link_to "#{friend.first_name.capitalize} #{friend.last_name.capitalize}", user_path(friend)%>
  </li>
<% end %>